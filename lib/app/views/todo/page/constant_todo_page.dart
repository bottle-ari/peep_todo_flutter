import 'package:flutter/material.dart';

import '../../common/base/basebody.dart';

class ScheduledTodoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BaseBody(widgetList: [
      Text("Constant Todo Page"),
    ]);
  }

}